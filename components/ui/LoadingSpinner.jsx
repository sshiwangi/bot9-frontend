const LoadingSpinner = () => (
  <div className="flex justify-center items-center h-full">
    <div className="loader" />{" "}
    {/* You can replace this with a spinner component from a library */}
  </div>
);
export default LoadingSpinner;
